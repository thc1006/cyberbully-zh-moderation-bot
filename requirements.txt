# CyberPuppy 中文網路霸凌防治系統 - 依賴包
#
# ⚠️ 重要提醒：首次安裝後請執行以下命令下載必需的大文件 (>100MB):
# python scripts/download_datasets.py
#
# 這些大文件因 GitHub 限制未包含在倉庫中，但對系統運行必不可少
# 詳細說明：docs/LARGE_FILES_SETUP.md

# Core dependencies
python-dotenv>=1.0.0
requests>=2.31.0
tqdm>=4.66.0
tenacity>=8.2,<9

# Hugging Face
datasets>=2.14.0
transformers>=4.35.0
tokenizers>=0.15.0

# Data processing - Windows Compatible
pandas>=2.1.0
# Windows: Use pre-compiled wheels to avoid GCC compilation issues
numpy>=1.24.4,<2.0.0; python_version < "3.12"  # Stable pre-compiled wheels
numpy>=1.26.4,<3.0.0; python_version >= "3.12"  # Support Python 3.13 with numpy 2.x+
# Windows: Use reimplemented version to avoid OpenCC C++ compilation
opencc-python-reimplemented>=0.1.7  # 繁簡轉換 (Windows compatible)
jieba>=0.42.1  # 中文分詞（備用）

# Data visualization and analysis (needed for model evaluation)
matplotlib>=3.8.0
seaborn>=0.13.0
plotly>=5.18.0

# Machine Learning - Windows Compatible
# Windows: Use CPU version or specify CUDA version explicitly to avoid compilation
torch>=2.1.0,<3.0.0
scikit-learn>=1.3.0,<2.0.0

# NLP Tools (optional but recommended)
# ckiptagger>=0.2.1  # 中研院斷詞工具（需要較大空間）

# API and Web
fastapi>=0.104.0
uvicorn>=0.24.0
pydantic==2.6.3  # Pin specific version for stability
pydantic-settings==2.2.1  # Pin specific version for BaseSettings in Pydantic v2
line-bot-sdk>=3.5.0  # LINE Bot SDK

# Explainability - Windows Compatible
# Windows: These packages may require pre-compiled wheels
shap>=0.48.0,<1.0.0  # Updated for Python 3.13 compatibility
captum>=0.8.0,<1.0.0  # PyTorch 可解釋性工具，支援 Python 3.13
numba>=0.58.0,<1.0.0  # Required by SHAP
cloudpickle>=3.0.0,<4.0.0  # Required by SHAP

# Testing (dev-only deps moved to requirements-dev.txt)

# Development tools (moved to requirements-dev.txt)

# Optional: Perspective API client
# google-api-python-client>=2.108.0

# Logging and monitoring
loguru==0.7.2  # Pin specific version for stability
rich>=13.5.0,<14.0.0  # Allow broader range for compatibility with dependencies