# Development Dependencies
# 開發環境所需的額外套件 (僅限開發工具，運行時依賴請放在 requirements.txt)

# 解決依賴衝突的明確約束 (2025年9月最新兼容版本)
urllib3>=2.0.0,<3.0.0  # Latest urllib3 2.5.0 compatible with modern packages

# 額外測試框架 (基本測試框架在 requirements.txt)
pytest-benchmark>=4.0.0,<5.0.0
pytest-watch>=4.2.0,<5.0.0
pytest-mock>=3.12.0,<4.0.0
pytest-timeout>=2.2.0,<3.0.0
pytest-xdist>=3.5.0,<4.0.0  # 平行測試
hypothesis==6.100.0  # Pin specific version to avoid resolution issues

# 額外程式碼品質工具 (基本工具在 requirements.txt)
flake8-docstrings==1.7.0
flake8-bugbear==23.12.2
flake8-comprehensions==3.14.0
pylint==3.0.3        # 程式碼分析
isort==5.13.2        # import 排序
autopep8==2.0.4      # PEP8 自動修正

# 型別檢查擴展
types-requests==2.32.0.20240914  # Pin to specific version to avoid resolution issues
types-PyYAML==6.0.12.20250822  # Latest (Aug 22, 2025)
types-python-dateutil==2.9.0.20250822  # Latest (Aug 22, 2025)

# 安全掃描 (2025年9月最新兼容版本)
bandit[toml]>=1.7.6,<1.8.0  # Allow minor version updates for compatibility
safety==3.2.8  # Pin specific version to avoid resolution issues
semgrep>=1.137.0,<2.0.0  # Latest version (Sep 24, 2025) with urllib3 2.x support

# 文檔工具
pydocstyle==6.3.0    # Docstring 檢查
radon==6.0.1         # 程式碼複雜度分析
sphinx==7.2.6        # 文檔生成
sphinx-rtd-theme==2.0.0
sphinx-autodoc-typehints==1.25.2
myst-parser==2.0.0

# 開發輔助工具
ipython==8.19.0
ipdb==0.13.13        # 調試器

# 效能分析
memory-profiler==0.61.0
line-profiler==4.1.2
locust==2.20.0       # 負載測試
py-spy==0.3.14       # 效能分析器

# Git hooks
pre-commit==3.6.0

# 授權檢查
pip-licenses==4.3.4

# 程式碼覆蓋率
coverage[toml]==7.4.0
codecov==2.1.13

# Mock 與測試資料
faker==22.0.0        # 假資料生成
factory-boy==3.3.0   # 測試資料工廠
responses==0.24.1    # HTTP mock
freezegun==1.4.0     # 時間 mock

# API 測試
httpx>=0.25.2,<0.26.0  # Allow patch updates
httpie>=3.2.2,<4.0.0  # Allow minor updates for better compatibility

# 開發輔助記錄
colorlog==6.8.0      # 彩色日誌

# 環境管理輔助
environs==11.0.0

# 程式碼生成
cookiecutter==2.5.0  # 專案模板

# 除錯與監控
watchdog==3.0.0      # 檔案監控
python-json-logger==2.0.7

# 發布工具
twine==4.0.2         # PyPI 上傳
wheel==0.42.0        # Wheel 打包
setuptools==69.0.3

# CI/CD 輔助
tox==4.11.4          # 多環境測試
nox==2023.4.22       # 任務自動化

# 資料庫工具（如需要）
# alembic==1.13.1    # 資料庫遷移
# sqlalchemy==2.0.25 # ORM

# 非同步測試輔助 (解決 resolution-too-deep 問題)
aiohttp>=3.8.0,<4.0.0      # 明確約束 aiohttp 版本
yarl>=1.7.0,<2.0.0         # 明確約束 yarl 版本
aioresponses==0.7.6        # 非同步 HTTP mock
pytest-aiohttp==1.0.5